<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Developer Skills</title>
</head>
<body>
    <h1>My Amazing Developer Skills</h1>
     <ul>
       <% skills.forEach(function(t) { %>
         <li>
            <a href="/skill/<%= t.id %>"><%= t.skills %></a>
           <%= t.skill %>
           
         </li>
       <% }); %>
     </ul>
</body>
</html>